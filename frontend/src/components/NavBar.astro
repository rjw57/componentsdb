---
import NavbarContainer, { NavbarDynamicContent } from "./NavbarContainer";
---

<NavbarContainer pathname={Astro.url.pathname} client:load>
  <!-- We use transition:persist here to stop dynaic content flashing on page navigation. -->
  <NavbarDynamicContent client:only="react" transition:persist />
</NavbarContainer>
